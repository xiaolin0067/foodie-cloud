# Eureka注册中心
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:20000/eureka/

spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-server
      profile: dev
      # 默认也是master
      label: master